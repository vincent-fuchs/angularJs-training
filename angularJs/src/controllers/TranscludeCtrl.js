angular.module('MyApp.controllers')
    .controller('TranscludeCtrl',['$scope',
        function ($scope) {
            'use strict';
            
            $scope.message="Message set by the controller";
            
            
         }]);
